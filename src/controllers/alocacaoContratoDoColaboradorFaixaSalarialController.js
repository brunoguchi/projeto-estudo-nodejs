const alterarFaixaSalarialProfissionalDiaLetivo =
    (request, response) => {
        //return response.status(400).json(['deu grosópi']);
        return response.status(200).json();
    };

module.exports =
{
    alterarFaixaSalarialProfissionalDiaLetivo
};
